# ---
# name: hello-world
# on:
#   push:
#     branches: [ main, feature/** ] # uncomment when running on runner
# jobs:
#   build-and-push:
#     name: hello-world
#     runs-on: self-hosted
#     steps:
#     - name: Checkout
#       uses: actions/checkout@v2 

    # - name: Configure AWS credentials from AWS account
    #   uses: aws-actions/configure-aws-credentials@v1
    #   with:
    #     aws-region: eu-west-2
    # - run: aws sts get-caller-identity

    # - name: terraform init
    #   run: |
    #     make init

    # - name: terraform plan
    #   run: |
    #     make plan

    # - name: terraform apply
    #   run: |
    #     make apply